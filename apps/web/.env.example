# Copia este archivo a .env y rellena. .env está en .gitignore (NUNCA se sube).

# URL del backend. En local: http://localhost:3001/api. En producción: la del servicio API de Railway.
NEXT_PUBLIC_API_URL="http://localhost:3001/api"

# Solo para regenerar los assets de imagen con OpenAI (scripts/generate-images.mjs). Opcional.
OPENAI_API_KEY=
